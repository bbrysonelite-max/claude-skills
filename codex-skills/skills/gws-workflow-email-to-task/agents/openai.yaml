interface:
  display_name: "Gws Workflow Email To Task"
  short_description: "Google Workflow: Convert a Gmail message into a Google Tasks..."
  default_prompt: "Use $gws-workflow-email-to-task to handle this request using its documented workflow."
