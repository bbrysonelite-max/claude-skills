interface:
  display_name: "Cloud Run Reauth"
  short_description: "Restore and use gcloud access to TigerClaw prod (Cloud Run +..."
  default_prompt: "Use $cloud-run-reauth to handle this request using its documented workflow."
